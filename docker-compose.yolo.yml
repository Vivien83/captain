services:
  captain:
    environment:
      - CAPTAIN_PROFILE=yolo
    network_mode: host
    privileged: true
    volumes:
      - /:/host:rw
      - /var/run/docker.sock:/var/run/docker.sock
    pid: host
    ipc: host
