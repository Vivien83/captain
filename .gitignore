# Build
/target
/dist/releases/
/dist/site/
/dist/docker/
/site/releases/
**/*.rs.bk
*.pdb

# Environment & secrets
.env
.env.*
!.env.example

# Database
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# User config (may contain API keys)
config.toml
# Exception: cargo build config (no credentials)
!.cargo/config.toml

# Certificates & keys
*.pem
*.key
*.cert
*.p12
*.pfx

# Runtime artifacts
collector_hand_state.json
collector_knowledge_base.json
predictions_database.json
prediction_report_*.md
BUILD_LOG.md

# OS
.DS_Store
._*
Thumbs.db

# IDE & tools
.idea/
.vscode/
.claude/
*.swp
*.swo
*~
.serena/

# HORA workflow artifacts (snapshots, backup bundles, daemon state)
.hora/snapshots/
.hora/backups/
.hora/logs/
.hora/last-check-*
.hora/doc-sync-state-*.json
.hora/session-backup-state-*.json
.hora/backup-state.json
.hora/backup-log.md
.hora/checkpoint.md
.hora/project-id

# Local launch-site design drafts generated during exploration. The committed
# launch surface is site/index.html only while marketing stays frozen.
site/proposals/
image.png

# Local ML model cache (fastembed)
.fastembed_cache/
**/.fastembed_cache/

# Session-only dev rapports / reprise files. These contain coordination notes
# between Claude Code and Codex sessions, not durable docs. Keep them locally
# but never push.
RAPPORT_*.md
REPRISE_POST_COMPACT*.md
TODO_CHANTIER_*.md
rapport_codex.md
